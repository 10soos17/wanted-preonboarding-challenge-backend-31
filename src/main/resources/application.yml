server:
 port: 8888

logging:
  level:
    root: info
---
spring:
  datasource:
      driver-class-name: org.postgresql.Driver
      url: jdbc:postgresql://localhost:5432/postgres
      username: postgres
      password: xxx
mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.cqrs.demo